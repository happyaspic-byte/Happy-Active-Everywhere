name: Verify and package

on:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: verify-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Install stable Rust
        shell: bash
        run: |
          rustup toolchain install stable --profile minimal --component rustfmt --component clippy
          rustup default stable
          rustc --version --verbose
          cargo --version
      - name: Format
        # Temporary dependency resolution gate; the changed lockfile is reviewed
        # and committed before any candidate can pass the clean-tree check.
        # Removed once the reviewed dependency update is locked.
        run: |
          cargo check --all-targets
          python -c "import base64,pathlib; print('REVIEW_LOCKFILE=' + base64.b64encode(pathlib.Path('Cargo.lock').read_bytes()).decode())"
      - name: Check committed formatting and dependency lock
        run: |
          cargo fmt --all
          git diff --exit-code
      - name: Lint
        if: ${{ !cancelled() }}
        run: cargo clippy --locked --all-targets -- -D warnings
      - name: Tests
        if: ${{ !cancelled() }}
        run: cargo test --locked --all-targets --no-fail-fast -- --nocapture
      - name: Release build
        run: cargo build --release --locked
      - name: Upload binary and verification documents
        uses: actions/upload-artifact@v4
        with:
          name: everywhere-${{ matrix.os }}-${{ github.sha }}
          path: |
            target/release/everywhere
            target/release/everywhere.exe
            README.md
            docs/
          if-no-files-found: error
          retention-days: 14
